'''
Instance variables
Instance methods
'''


"""def func1():
    print("Funciton1 body")


func1()


class Employee:
    # local variables   - eid, name, sal
    # instance variables - self.eid self.name self.sal - instance variables

    def __init__(self, eid, name, sal):
        print("Self address : ", self)
        self.eid = eid
        self.name = name
        self.sal = sal

    # instance methods
    def get_edata(self):
        print("Employee information : ", self.eid, self.name, self.sal)


madhu = Employee(100, 'MadhuN', 15000)
madhu.get_edata()  # Employee.get_edata(madhu)

li1 = [1, 2, 3]  # list1 is of type class list
li1.append(100)  # list.append(li1,100)
li1.pop()  # list.pop(li1)"""

"""class Student:

    def __init__(self, id, name, age, email, phonenumber):
        print("Enter the student details :", self)
        self.id = id
        self.name = name
        self.age = age
        self.email = email
        self.phonenumber = phonenumber

        # instance method

    def get_student(self):
        print("Student information :", self.id, self.name, self.age, self.email, self.phonenumber)

satish = Student(101,"satish",23,"satish143@gmail.com","9675657362")
satish.get_student()"""


"""class Employee:
    def __init__(self,name,id,location,gender,salary):
        print("Enter the Details employee :",self)
        self.name = name
        self.id = id
        self.location = location
        self.gender = gender
        self.salary = salary

    # instance method
    def get_Employee(self):
        print("Enter the employee Details: ",self.name,self.id,self.location,self.gender,self.salary)

satish = Employee("satish",101,"Hyderabad","Male","45000")
satish.get_Employee()
"""
"""
class Tiles:

    #State
    def _init_(self,t_bname,t_price,t_clr):
        self.t_bname=t_bname
        self.t_price=t_price
        self.t_clr=t_clr
 #Behavior
    def get_info(self):
     print("info of about Laptop  " ,self.t_bname, self.t_price,self.t_clr)
t_info=Tiles("somany" , 650,"Fossil Natural")
t_info.get_info()
"""

"""
class Lipstick:

    #State
    def _init_(self,bname,price,clr):
        self.bname=bname
        self.price=price
        self.clr=clr
 #Behavior
    def get_info(self):
     print("info of about Laptop  " ,self.bname, self.price,self.clr)
lp_info=Lipstick("Estee Lauder" , 3650,"sting")
lp_info.get_info()
"""

"""
class Bottle:

    #State
    def _init_(self,bname,price,clr):
        self.bname=bname
        self.price=price
        self.clr=clr
 #Behavior
    def get_info(self):
     print("info of about Bottle  " ,self.bname, self.price,self.clr)
b_info=Bottle("bkr" , 2600,"white")
b_info.get_info()
"""

"""
class Vegetable:

    #State
    def _init_(self,vname,vprice,Calories ):
        self.vname=vname
        self.vprice=vprice
        self.Calories=Calories
 #Behavior
    def get_info(self):
     print("info of about vegetable  " ,self.vname, self.vprice,self.Calories)
v_info=Vegetable("Ladies Finger" , 30, 20.5)
v_info.get_info()
"""

"""
class Bag:

    #State
    def _init_(self,bname,price,clr):
        self.bname=bname
        self.price=price
        self.clr=clr
 #Behavior
    def get_info(self):
     print("info of about Bag  " ,self.bname, self.price,self.clr)
b_info=Bag("Safari" , 2600,"Blue+yellow")
b_info.get_info()
"""

"""
class Bag:

    #State
    def _init_(self,bname,price,clr):
        self.bname=bname
        self.price=price
        self.clr=clr
 #Behavior
    def get_info(self):
     print("info of about Bag  " ,self.bname, self.price,self.clr)
b_info=Bag("Safari" , 2600,"Blue+yellow")
b_info.get_info()
"""

"""
class Table:

    #State
    def _init_(self,bname,price,clr):
        self.bname=bname
        self.price=price
        self.clr=clr
 #Behavior
    def get_info(self):
     print("info of about Table  " ,self.bname, self.price,self.clr)


t_info=Table("Furniture" , 25000,"brown")
t_info.get_info()
"""
